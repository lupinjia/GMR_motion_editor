# GMR可视化编辑器设计文档

**创建日期:** 2025-03-23  
**版本:** 1.0  
**状态:** 已批准

---

## 1. 项目概述

### 1.1 目标
为GMR (General Motion Retargeting)项目开发一个图形化界面工具，用于可视化、编辑和导出机器人运动数据。

### 1.2 核心功能
- **导入/导出**: 加载和保存GMR格式(.pkl)的机器人运动数据
- **可视化**: 使用MuJoCo实时显示机器人运动
- **剪辑**: 简单的起止时间裁剪功能
- **多机器人支持**: 支持项目中所有17种机器人模型

---

## 2. 架构设计

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    主窗口 (PyQt6 MainWindow)                   │
│  ┌─────────────┐  ┌──────────────────────────┐  ┌─────────┐ │
│  │ 菜单栏       │  │      控制面板            │  │ 状态栏  │ │
│  │ File Edit   │  │  [播放/暂停] [帧数显示]   │  │  状态   │ │
│  └─────────────┘  │  [速度滑块] [循环开关]    │  └─────────┘ │
│                   └──────────────────────────┘             │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │              时间轴控制区                                │ │
│  │  ◄═══════════════════════════════►                      │ │
│  │    [裁剪起点] [裁剪终点] [当前帧指示器]                   │ │
│  │    [总时长: XXs] [当前: XXs]                             │ │
│  └─────────────────────────────────────────────────────────┘ │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │              机器人选择区                                │ │
│  │  机器人: [Unitree G1 ▼]  FPS: 30  帧数: 120             │ │
│  │  DOF: 29  时长: 4.0s                                    │ │
│  └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│              MuJoCo Viewer (独立窗口)                        │
│         实时显示机器人运动                                    │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 核心组件

| 组件 | 文件 | 职责 |
|------|------|------|
| MainWindow | `gui/main_window.py` | PyQt6主窗口，包含所有控件，协调数据流，状态管理 |
| TimelineWidget | `gui/timeline_widget.py` | 自定义QWidget，显示滑动条、裁剪标记手柄、当前帧指示器 |
| MotionController | `gui/motion_controller.py` | 播放控制逻辑，帧率管理，与MuJoCo viewer通信 |
| GMRDataManager | `gui/gmr_manager.py` | 加载/保存GMR pickle文件，裁剪数据，元数据提取 |
| RobotSelector | `gui/widgets.py` | 机器人选择下拉框，复用`params.py`中的机器人列表 |

---

## 3. 数据流设计

### 3.1 播放循环
```
1. QTimer触发 (间隔 = 1000ms/FPS)
2. 获取当前帧数据 (root_pos, root_rot, dof_pos)
3. 发送到MuJoCo viewer显示
4. 更新TimelineWidget当前位置
5. 到达裁剪终点时暂停或循环
```

### 3.2 裁剪操作
```
1. 用户调整起点/终点手柄
2. TimelineWidget发出信号
3. MainWindow更新裁剪范围
4. 导出时，GMRDataManager截取[start:end]区间数据
```

### 3.3 数据格式
- 复用现有pickle格式：
  - `fps`: 帧率
  - `root_pos`: 根节点位置 [N, 3]
  - `root_rot`: 根节点旋转 [N, 4] (xyzw格式)
  - `dof_pos`: 关节位置 [N, n_dof]
  - `local_body_pos`: 局部身体位置
  - `link_body_list`: 链接身体列表
- 裁剪后保持原有字段不变，只截取帧范围

---

## 4. 界面设计

### 4.1 主窗口规格
- **尺寸:** 800x600px
- **技术栈:** PyQt6
- **3D视图:** 独立MuJoCo窗口

### 4.2 菜单栏
```
File:
  - Open (Ctrl+O)
  - Save (Ctrl+S)
  - Save As
  - Export Clip
  - Exit

Edit:
  - Undo
  - Redo
  - Reset View

View:
  - Toggle Loop
  - Reset Timeline

Help:
  - About
```

### 4.3 时间轴控件
- **进度条:** 显示总帧数（如 0 - 120）
- **起点手柄:** 蓝色，可拖动
- **终点手柄:** 红色，可拖动
- **当前帧指示器:** 黄色竖线，随播放移动
- **数字输入框:** 精确设置起止帧

### 4.4 控制面板
- 播放/暂停按钮
- 速度滑块 (0.5x - 2.0x)
- 循环开关
- 当前帧/总帧数显示
- 时间显示 (秒)

### 4.5 状态栏
- **左侧:** 当前状态（就绪/播放中/裁剪中）
- **右侧:** 文件路径、机器人名称、总帧数

---

## 5. 错误处理

| 错误场景 | 处理方式 |
|---------|---------|
| 文件不存在/损坏 | QMessageBox提示，恢复到之前状态 |
| 机器人模型缺失 | 提示下载，提供安装指南链接 |
| 裁剪范围无效 | 自动修正（起点>终点时交换），提示用户 |
| MuJoCo窗口关闭 | 自动停止播放，重新打开时恢复 |
| 内存不足 | 分段加载，显示加载进度 |

---

## 6. 技术选型

### 6.1 GUI框架: PyQt6
- **选择理由:** 
  - 与现有GMR项目兼容（已有PyQt6可视化代码）
  - 成熟稳定，生态丰富
  - 支持自定义控件

### 6.2 3D渲染: MuJoCo
- **选择理由:**
  - 项目已有成熟的MuJoCo集成
  - RobotMotionViewer可直接复用
  - 机器人渲染效果最佳

---

## 7. 项目结构

```
GMR/
├── general_motion_retargeting/
│   └── gui/                    # 新增GUI模块
│       ├── __init__.py
│       ├── main_window.py
│       ├── timeline_widget.py
│       ├── motion_controller.py
│       ├── gmr_manager.py
│       └── widgets.py          # 通用控件
├── scripts/
│   └── gmr_visualizer.py       # 启动脚本
└── README.md                   # 更新使用说明
```

---

## 8. 依赖

### 8.1 新增依赖
```bash
pip install PyQt6
```

### 8.2 复用依赖
- 所有现有的GMR依赖（mujoco, numpy, pickle等）

---

## 9. 支持的机器人

复用`general_motion_retargeting/params.py`中的所有机器人：

| ID | 机器人名称 | DOF |
|----|-----------|-----|
| 0 | unitree_g1 | 29 |
| 1 | unitree_g1_with_hands | 43 |
| 2 | unitree_h1 | 19 |
| 3 | unitree_h1_2 | 27 |
| 4 | booster_t1 | - |
| 5 | booster_t1_29dof | - |
| 6 | booster_k1 | 22 |
| 7 | stanford_toddy | - |
| 8 | fourier_n1 | - |
| 9 | engineai_pm01 | - |
| 10 | hightorque_hi | 25 |
| 11 | galaxea_r1pro | 24 |
| 12 | kuavo_s45 | 28 |
| 13 | berkeley_humanoid_lite | 22 |
| 14 | pnd_adam_lite | 25 |
| 15 | tienkung | 20 |
| 16 | pal_talos | 30 |
| 17 | fourier_gr3 | 31 |

---

## 10. 用户操作流程

### 10.1 基本使用
1. 运行 `python scripts/gmr_visualizer.py`
2. File → Open 选择 .pkl 文件
3. 选择对应的机器人类型
4. 点击播放按钮查看运动
5. 调整时间轴手柄设置裁剪范围
6. File → Export Clip 导出裁剪后的数据

### 10.2 快捷键
- `Ctrl+O`: 打开文件
- `Ctrl+S`: 保存
- `Space`: 播放/暂停
- `←/→`: 上一帧/下一帧
- `Home`: 跳到开头
- `End`: 跳到结尾

---

## 11. 扩展性考虑

### 11.1 未来可扩展功能
- 多机器人并排对比
- 波形图显示（显示关节角度变化）
- 关键帧标记和跳转
- 批量处理多个文件
- 导出为视频格式

### 11.2 代码设计原则
- 模块化：各组件职责清晰，接口明确
- 松耦合：组件间通过信号/槽通信
- 可测试：核心逻辑可独立测试

---

## 12. 附录

### 12.1 GMR数据格式参考
详见 `general_motion_retargeting/data_loader.py`

### 12.2 MuJoCo Viewer参考
详见 `general_motion_retargeting/robot_motion_viewer.py`

---

**文档维护:** 如有需求变更，请更新此文档并重新审核。
